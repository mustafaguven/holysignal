<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mguven.holysignal">

    <application
        android:name="com.mguven.holysignal.TheApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.mguven.holysignal.ui.MainActivity"
            android:theme="@style/NoActionBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.mguven.holysignal.ui.CardActivity"
            android:theme="@style/NoActionBarTheme" />

        <activity android:name="com.mguven.holysignal.ui.DownloadActivity" />

        <activity
            android:name="com.mguven.holysignal.ui.LoginActivity"
            android:theme="@style/NoActionBarTheme" />

        <activity
            android:name="com.mguven.holysignal.ui.SignupActivity"
            android:theme="@style/NoActionBarTheme" />

        <!-- The below code is for android OS version below N -->
        <receiver
            android:name=".util.ConnectivityReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

    </application>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>